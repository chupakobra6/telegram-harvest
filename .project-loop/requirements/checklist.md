# Чеклист Требований

Проект: telegram-harvest
Обновлено: 2026-07-31

## Значения Статусов
Используй `кандидат`, `принято`, `в работе`, `готово`, `отложено`, `заблокировано` или `отклонено`.

## Требования
| ID | Статус | Источник | Требование | Критерий приемки | Доказательства |
| --- | --- | --- | --- | --- | --- |
| REQ-001 | `готово` | S001, S003 | `daily-catchup` читает весь новый диапазон одним Telegram range-scan и затем разбивает записи по дневным отчетам. | Один вызов range collector на весь список новых jobs; каждый день получает только записи своего `[start,end)`. | `runDailyRangeJobs`; unit-тест single-call/partition/gaps; live run: один range, 70 batches, 8 дней. |
| REQ-002 | `готово` | S001, S002 | Новый flow сохраняет содержимое и формат daily: self, Trackmate только в Haru, форварды, медиа, транскрипты и merged catch-up. | Сравнение старого и нового результата на одинаковом диапазоне не показывает потери или лишние сообщения; существующие contract-тесты проходят. | Все 1 742 baseline records сохранены, 0 semantic mismatches, дополнительно найдены 22 исходящих сообщения; scope: 1 706 self/outgoing, 58 Trackmate, 0 other incoming. |
| REQ-003 | `готово` | S001 | Эффект измерен воспроизводимо на одном диапазоне. | Зафиксированы wall time, диапазон, количество дней/сообщений и отношение old/new; прогоны используют один профиль, ASR-кэш и одинаковые параметры. | 2026-07-22—2026-07-29: 290.77 s → 60.28 s, 4.82×; повторные range-runs 55.25 s и 54.74 s. |
| REQ-004 | `готово` | S004, S005 | Daily flow напрямую измеряет Telegram scan, media download, ffmpeg, Vosk и render. | Каждая стадия замеряется у места выполнения, включая failed work; timing report содержит wall total и stage work без восстановления данных из ASR JSONL; overlap описан отдельно. | `internal/stages`; observers в MTProto/transcribe/render; success/failure tests; pipeline span/overlap metrics. |
| REQ-005 | `готово` | S004 | Stage timings сохраняются как отдельный per-run structured artifact. | Успешный `daily`/`daily-catchup` печатает timings и атомарно сохраняет уникальный JSON report под `.state/daily/timings`; новый прогон не перезаписывает предыдущий. | `stage_timings.go`; unit test сохраняет два разных reports и проверяет первый; live report `20260730T074558.861993000Z-30261-1-daily-catchup.json`. |
| REQ-006 | `готово` | S005 | Daily media обрабатывается bounded pipeline без конкурентных Telegram RPC. | Telegram producer продолжает scan/download после enqueue; локальные jobs выполняются конкурентно; очередь ограничена и создает backpressure. | `media_pipeline.go`; queue backpressure test; live overlap 33.82–35.67 s при прежних 56 последовательных Telegram batches и 0 FloodWait. |
| REQ-007 | `готово` | S005 | Vosk workers независимы, а auto-controller выбирает безопасное эффективное число от 1 до 4. | Каждый worker имеет отдельный process/model/session; auto расширяется только при доказанной выгоде и достаточных ресурсах; diagnostic override поддерживает 1..4. | Auto resource tests; live auto выбрал 2, RSS 570–913 MiB/worker, fixed 4 не улучшил wall. |
| REQ-008 | `готово` | S005 | Кэш, dedup и collector не меняют содержимое или порядок отчетов. | Cache hit не создает job; одинаковый in-flight media выполняется один раз; atomic cache write; sequential и pipeline JSONL структурно совпадают. | Dedup/atomic/failure tests; normalized JSONL hash одинаков во всех 5 cold modes; Markdown SHA-256 одинаков без нормализации; temp files 0. |
| REQ-009 | `готово` | S005 | Timing report честно описывает перекрывающиеся стадии и ресурсы workers. | Есть pipeline span/overlap, requested/activated/peak workers, per-worker startup/RSS/jobs/audio/ffmpeg/Vosk/speed, queue peak; work-seconds не выдаются за wall decomposition. | `media_pipeline` timing object; stage work replaces invalid unaccounted arithmetic; live reports inspected for 1/2/4/auto and warm cache. |
| REQ-010 | `готово` | S006 | Транскрипция использует единый typed ASR backend contract с реализациями Vosk CPU и whisper.cpp. | Daily pipeline не знает деталей запуска движка; timing/cache identity включают backend, модель, вариант ускорения и существенные параметры декодирования. | `Descriptor`, `WorkerPolicy`, managed runners; cache изолирован по backend/binary/model/accelerator/language/threads/grammar/VAD; contract tests. |
| REQ-011 | `готово` | S006 | Worker policy зависит от backend: Vosk CPU может динамически масштабироваться, whisper.cpp Metal/Core ML в auto начинает и остаётся с одним GPU worker. | Auto не запускает конкурирующие GPU workers; явный diagnostic override отделён от безопасного default и виден в metrics. | Policy/pipeline tests; live timing: `worker_resource=gpu`, `dynamic_workers=false`, requested/activated/peak = 1. |
| REQ-012 | `готово` | S006 | Для whisper.cpp поддерживаются Metal и Metal + Core ML encoder, multilingual models и quantized model files без смешивания результатов. | Backend сообщает фактический accelerator/model identity; Core ML/Metal readiness проверяется; кэш одного варианта никогда не обслуживает другой. | Официальный whisper.cpp `4523d0ce`; Metal/Core ML builds; runtime checks подтвердили Apple M4 Pro, Metal device, `COREML=1` и `Core ML model loaded`; cache collision tests. |
| REQ-013 | `готово` | S006 | Один реальный локальный корпус сравнивает backend-варианты по производительности и качеству. | Для каждого варианта сохранены audio seconds, ASR/pipeline speed, cold-start, peak RSS, CPU и доступная GPU evidence, WER/CER, empty/error counts; одинаковые входы/reference и нормализация. | Corpus 170.284 s/hash `ba03ca…16c4`; 6 variants × 3 fresh processes; current JSON + documented table/transcripts; GPU percent explicitly unavailable without elevated powermetrics. |
| REQ-014 | `готово` | S007 | Daily не использует неполный `messages.search` и не теряет исходящие сообщения. | Единственный per-dialog source — последовательный `messages.getHistory`; sender scope фильтруется локально; проблемное `1221157785:415830` возвращается. | Search path удалён; history-only live вернул 211/211 keys и `415830`; full Whisper E2E подтвердил 0 missing/extra/semantic mismatches. |
| REQ-015 | `готово` | S008 | Benchmark использует расширенный приватный корпус разнообразных исходящих голосовых Игоря и неречевых Telegram-медиа. | Корпус покрывает короткие/длинные сообщения, разговорную и техническую речь, имена/англицизмы, разный темп и фон; исходники остаются только в `.state`; состав и hash воспроизводимы. | 28 voice + 2 weak-speech video + 12 non-speech controls, 2178.413 s; voice 5.1–150.2 s, median 76.3 s; hash `d79e32…24c1`; все private bytes/results ignored. |
| REQ-016 | `готово` | S008, S009 | Whisper pipeline сравнивает обоснованные настройки качества и подавления галлюцинаций без опасной потери речи. | Кандидаты включают decoder/no-speech/non-speech/VAD настройки только с полным cache identity; отдельно измеряются content errors, critical deletions/negations, non-speech false positives, скорость и ресурсы. | 10-way tuning + full Vosk/small/turbo/large/beam comparison; content/negation/number metrics; whole-file Silero gate 0/12 hallucinations и 0/30 misses; v1.9.1 argument bug covered; terminal filter removed 2 verified boilerplates. |
| REQ-017 | `готово` | S008 | Для daily выбирается и закрепляется ровно один итоговый ASR production-профиль. | Победитель стабильно лучше Vosk по содержанию, не имеет неприемлемых semantic deletions, укладывается в практический runtime; default/config/docs/cache согласованы и live daily сохраняет contract. | Production: turbo q5_0 Metal + beam 5 + whole-file Silero; relative WER 10.58% vs Vosk 36.14%, number recall 94.03% vs 46.27%; 3-run median 13.63×; `.env`/CLI/docs/cache/live daily согласованы. |
| REQ-018 | `готово` | S010 | Daily timing report различает history data pages, пустые proof pages и разреженные continuation pages. | Метрики считаются на реальных ответах history, сохраняются в per-run JSON/CLI и не меняют report content. | `history_pagination` и CLI counters; integrated timing JSON: 96 data, 2 empty proof, 0 sparse; unit persistence/readback зелёный. |
| REQ-019 | `готово` | S010, S002 | Checkpoint scan не делает пустой второй history RPC, когда полнота bounded range формально доказана, и сохраняет старый безопасный путь во всех остальных случаях. | Оптимизация действует только для непрерывного валидного checkpoint с `MinID`; sparse/inexact/unknown, historical, first run, gap и mismatch используют полный fallback; shadow verifier и regression tests не показывают пропусков. | Exact response/head/MinID proof; fallback tests; live 4/4 shadow confirmed, 0 rejected; enforced 21→17 batches, 45-record JSONL byte-identical. |
| REQ-020 | `готово` | S010, S011 | Main/daily и study используют единый статический code-owned RPC spacing, выбранный live-калибровкой, а не постоянно меняющийся adaptive controller. | Несколько одинаковых прогонов последовательно проверяют 700/600/550/500/450 ms; выбранный floor имеет 0 FloodWait, 0 Telegram errors и структурно идентичный JSONL, с запасом относительно первого нестабильного уровня. | Общий floor 500 ms: 3× clean 103-RPC main runs; 400 и повторный 450 дали FloodWait; пользователь явно выбрал тот же default для study; normalized JSONL hash одинаков. |
| REQ-021 | `готово` | S012 | Репозиторий проходит системный code/architecture review без известных correctness, static-analysis или reachable-vulnerability findings. | Исправлены безопасные локальные findings; крупные границы оценены без косметического усложнения архитектуры. | Удалён redundant control flow; Go 1.26.5 и `x/net` 0.53.0; Staticcheck clean; Govulncheck 0 reachable vulnerabilities. |
| REQ-022 | `готово` | S012 | Setup и validation имеют один воспроизводимый developer contract локально и в GitHub CI. | `make setup` не мутирует module files; `make check` проверяет format/tidy/vet/tests; `make audit` запускает pinned static/security tools; CI повторяет check/audit/race. | Make targets выполнены локально; workflow использует Go из `go.mod` и read-only permissions. |
| REQ-023 | `готово` | S012 | Help, README и agent rules одинаково объясняют state-dir-relative low-level paths и защищают private generated outputs от git. | Нет рекомендаций вида `.state/.state/*`; реальные default output paths и ignore policy совпадают с кодом. | CLI flag help обновлён; README показывает `.state/messages*`/`.state/agent-view`; `chat.jsonl` и `media-manual/` ignored. |
| REQ-024 | `готово` | S013 | Daily выбирает безопасное число chunk workers для каждого Telegram media download по размеру и доказанной live-эффективности. | Один файл скачивается за раз; production policy физически ограничена 1/2, малые файлы остаются на 1, threshold/cap подтверждены одинаковым корпусом, SHA-256 и transport evidence; timing report показывает решения, throughput, retries/failures/floods. | Production: `<1 MiB → 1`, `>=1 MiB → 2`, hard cap 2; 4-worker probe отклонён после FloodWait; product override удалён. |
| REQ-025 | `готово` | S013 | Checkpoint сохраняет максимальную безопасную границу всех raw-сообщений, полностью проверенных до exclusive end диапазона, независимо от sender/output filter. | Нормальная граница монотонна и не перескакивает сообщения следующего дня; меняется только после complete scan и merged publish; неподтверждённый snapshot head сбрасывает dialog boundary в 0 для полного fallback; full и optimized JSONL совпадают, Trackmate/self не теряются. | Raw observe выполняется до sender filter; exclusive/incomplete/head-race/anomaly tests; live 270/270 keys, 7/7 Trackmate, одинаковый stable payload, 0 FloodWait. |
| REQ-026 | `готово` | S013 | Makefile собирает один локальный бинарник и переиспользует его до изменения Go sources или module files. | `make daily-catchup` не вызывает `go run`; первый запуск строит ignored binary, повторный сохраняет его mtime, изменение/добавление/удаление source вызывает rebuild; help/clean/docs согласованы. | `bin/telegram-harvest` + source-set stamp; first/no-op/content/add/delete smoke: stale/fresh exits корректны. |

## Ограничения
| ID | Статус | Источник | Ограничение | Доказательства |
| --- | --- | --- | --- | --- |
| CON-001 | `готово` | S002, S010, S011, S013 | History crawlers и high-level media downloads остаются последовательными и используют единый статический pacing; разрешён только bounded chunk parallelism внутри одного активного достаточно большого файла после live-доказательства. | Один `beforeRPC` scheduler и один producer; общий default 500 ms; одновременно скачивается не более одного файла; production helper возвращает только 1/2, terminal/retry FloodWait и transport floods учитываются. |
| CON-002 | `готово` | S002 | Нельзя расширять daily scope до остальных входящих сообщений Haru. | Структурная проверка: Trackmate 58, остальные incoming 0. |
| CON-003 | `готово` | S002 | Пользовательские report JSONL/Markdown публикуются атомарно; технические ASR-логи могут оставаться частичными при interruption; ZIP по умолчанию не создается. | Incomplete/error-path tests сохраняют старые reports; ASR error блокирует публикацию; ZIP не создавался. |

## Обязательная Валидация
| ID | Статус | Источник | Валидация | Доказательства |
| --- | --- | --- | --- | --- |
| VAL-001 | `готово` | S001 | Focused unit/integration tests для range partition, gaps, incomplete scan и ASR routing. | Добавлены тесты single-call, half-open boundaries, gaps, per-day limit, skipped media, incomplete publish, ASR routing и ASR error propagation. |
| VAL-002 | `готово` | S001, S002 | `gofmt`, `go test ./...`, `git diff --check`. | Все проверки зелёные на current head 2026-07-30. |
| VAL-003 | `готово` | S001 | Live old/new benchmark 2026-07-22—2026-07-29 и структурная сверка JSONL/Markdown. | 290.77 s baseline против 60.28 s range; final 54.74 s; 0 missing, 22 added, 0 common semantic mismatches; final Markdown идентичен предыдущему current-head run. |
| VAL-004 | `готово` | S004, S005 | Unit-тесты stage work/persistence и live `daily-catchup` с проверкой пяти полей timing report. | `go test ./...` зелёный; JSON содержит все stage fields, `stage_work_seconds`, wall total и отдельный pipeline object; live: 1 range, 70 batches, 1 764 records, 0 FloodWait. |
| VAL-005 | `готово` | S005 | Pipeline/controller/cache проходят focused, race, failure и repeated concurrency tests. | `go test ./...`, `go vet ./...`, `go test -race` и `TestMediaPipeline` ×20 зелёные; cancellation/error cleanup и bounded queue покрыты. |
| VAL-006 | `готово` | S005 | Cold-cache sequential/1/2/4/auto benchmark и structural equivalence. | 94.22 s sequential; 60.66 s fixed1; 54.56 s fixed2; 55.06 s fixed4; auto 54.95/55.61 s. 210 records, 3 ASR jobs, 170.284 s audio, 0 FloodWait, identical normalized JSONL/raw Markdown. |
| VAL-007 | `готово` | S006 | Backend/cache/policy/helper проходят unit, integration, race и failure tests. | `go test ./...`, `go vet ./...`, focused `-race`, `git diff --check` зелёные; fake long-lived server и cleanup/cache/policy tests. |
| VAL-008 | `готово` | S006 | Vosk, whisper.cpp Metal и whisper.cpp Core ML benchmark выполнен на одном реальном корпусе. | `.state/asr-benchmark/results-final-current.json`; 6 variants × 3, одинаковый corpus hash; cold/steady, performance/resources/quality и runtime evidence сохранены. |
| VAL-009 | `готово` | S006 | Daily catch-up с выбранным победителем сохраняет report contract и детерминизм. | Live turbo Metal: 54.383 s, 29.40× ASR, 16.69× pipeline, 1 worker, 0 FloodWait; 210 общих records, 0 semantic mismatches. Один прежний Telegram-search record отсутствует и воспроизведён также без ASR, поэтому явно отделён от backend validation. |
| VAL-010 | `готово` | S007 | History-only daily подтверждён тестами и повторным live прогоном проблемной даты. | Unit/full/race/vet; 211/211 keys, включая `415830`; 0 FloodWait; зафиксировано изменение batches/wall time. | No-ASR: 211, 92 batches, 70.530 s, 0 FloodWait. Full Whisper: 211, 21 attachments, 3 transcripts, 81.083 s, 0 semantic mismatches. Full/race/vet/diff/Project Loop зелёные. |
| VAL-011 | `готово` | S008 | Расширенный corpus benchmark выполняется одинаково для Vosk и всех Whisper-кандидатов. | Одинаковые media bytes/order, минимум один полный quality run и повторные performance runs финалистов; report сохраняет per-sample transcripts, content metrics, hallucinations и critical errors. | `.state/asr-quality-20260731/results-final-beam5-3runs.json`; 42 identical samples; independent large-v3 silver reference; 3 final repetitions; full per-sample diagnostics. |
| VAL-012 | `готово` | S008, S009 | Настройки против галлюцинаций проходят speech/non-speech regression. | Неречевые ложные тексты уменьшаются без потери отрицаний, чисел, имен и значимых фраз; небезопасные кандидаты отклоняются с примерами. | Greedy/no-speech/suppress/no-fallback/beam/gate thresholds compared; no-fallback repetition rejected; integrated cutting VAD rejected; final 0 missed, 0 control hallucinations, negation recall 97.95%, numbers 94.03%. |
| VAL-013 | `готово` | S008 | Итоговый профиль проверен unit/race/full tests и current-head live daily. | Cache isolation, deterministic report output, stage timings, один GPU worker, 0 FloodWait и неизменный Telegram/report contract подтверждены. | `go test ./...`, vet, focused race, pipeline ×20; live 2026-07-25: 211 records, 21 attachments, 2 useful transcripts, 1 gated control, 85.888 s, 13.39× pipeline, 1 GPU worker, 0 FloodWait; normalized JSONL SHA identical. |
| VAL-014 | `готово` | S010 | Pagination proof и новые метрики покрыты fake-response unit/integration tests. | Проверены exact short page, full page, sparse continuation, inexact response, invalid head/MinID, error/incomplete path и неизменный deterministic record set. | Focused proof/shadow/fallback/sparse tests и timing persistence зелёные; full/race suite зелёный. |
| VAL-015 | `готово` | S010, S011 | Статический pacing и итоговый optimized catch-up проверены focused/full/race тестами и live structural comparison. | Fake clock подтверждает последовательные интервалы; live calibration сохраняет 0 FloodWait/errors; финальный JSONL имеет 0 missing/extra/semantic mismatches относительно безопасного baseline. | 700→400 main matrix; единый 500 ms default; 211-record integrated CLI, 0 FloodWait, 56.783 s; normalized SHA `e62281eb…6277`; proof shadow/enforced byte-identical. |
| VAL-016 | `готово` | S012 | Repo-polish проходит full, race, static, security, module, help и workflow validation. | Все команды завершаются успешно; module graph tidy; reachable vulnerabilities отсутствуют; help отражает фактический path contract. | `make check`, `make audit`, `go test -race -count=1 ./...`, `go mod verify`, CLI/Make help smoke, `git diff --check` зелёные. |
| VAL-017 | `готово` | S013 | Один cold Telegram media corpus сравнивается с 1/2/4 и итоговым auto policy. | Для каждого варианта измерены wall/bytes/s, hashes, retry/flood/error counters; auto не хуже безопасного фиксированного кандидата на существенных файлах и не меняет bytes. | 5 файлов/124 401 837 bytes: 1=76.887 s, 2=40.033/38.684 s clean, 4=22.983 s + FloodWait (rejected), auto cap2=35.747 s clean; SHA-256 одинаковы. |
| VAL-018 | `готово` | S013 | Новая raw checkpoint boundary проходит behavior/failure/shadow и live full-vs-optimized проверку. | Входящие отфильтрованные сообщения двигают boundary; exclusive end и incomplete/error не двигают; anomaly вынуждает full dialog fallback; full/optimized keys и semantic JSONL совпадают. | Focused/full/race зелёные; isolated full/legacy/raw A/B; 270/270 keys, 7/7 Trackmate, 0 stable semantic mismatches и 0 FloodWait. |
| VAL-019 | `готово` | S013 | Переиспользуемый бинарник проверен Makefile integration test/smoke. | First build, no-op reuse и content/add/delete-triggered rebuild доказаны без изменения product output. | Source-set stamp: add/delete оба дают `make -q build` exit 1, после build exit 0; неизменный binary сохраняет mtime. |

## Границы Объема
| ID | Статус | Источник | Граница | Примечания |
| --- | --- | --- | --- | --- |
| SCOPE-001 | `принято` | S001 | Параллельный ASR, смена движка и снижение RPC spacing не входили в STEP-001. | STEP-001 ограничивался устранением повторных дневных Telegram-сканов; параллельный ASR позже отдельно принят в STEP-003 через S005. |
| SCOPE-002 | `принято` | S005 | STEP-003 не меняет ASR engine и Telegram pacing. | Vosk сохраняется; Telegram RPC/download строго последовательны и paced. |
| SCOPE-003 | `принято` | S006 | STEP-004 не меняет Telegram scan/download pacing и не пытается ускорять Vosk через Metal. | Изменения ограничены общим ASR contract, локальными движками, backend-specific pool, измерениями и выбором default по результатам. |
| SCOPE-004 | `принято` | S008 | STEP-006 не отправляет приватные аудио или транскрипты во внешние ASR/LLM API и не расширяет daily sender scope. | Все inference и evaluation выполняются локально; внешние источники используются только для research. |
| SCOPE-005 | `принято` | S010 | STEP-007 не меняет ASR/media quality, sender scope и не вводит конкурентные Telegram RPC или runtime-adaptive pacing. | Работа ограничена history completeness proof, диагностикой, статической калибровкой и соответствующей документацией. |
| SCOPE-006 | `принято` | S012 | STEP-008 не меняет Telegram/report/ASR contracts и не делает широкий механический split крупных файлов ради косметики. | Только доказанные security/correctness/DX fixes, validation automation, docs/help и минимальная cleanup правка. |
| SCOPE-007 | `принято` | S013 | STEP-009 оптимизирует недельный cold catch-up, не меняет ASR-модель/качество, sender scope, RPC pacing или атомарную публикацию и не оставляет benchmark flow в пользовательском CLI. | Приватные benchmark artifacts остаются во временном/ignored state и удаляются; production имеет один auto policy без новых пользовательских knobs. |
